# Rule File Selection Report

- Bundled default 與 Manual Explicit 兩種模式；Manual 必須逐一選四個 role，不做目錄猜測或靜默 fallback。
- Provider-visible：Common Rules、Skill Catalog、Rule Set Manifest。
- App-only：HTML Report Template。
- 每個 role 顯示檔名、版本、bytes、SHA-256 與 provider visibility。
- 四份 bundled SHA-256 與授權來源 byte-identical：PASS。
