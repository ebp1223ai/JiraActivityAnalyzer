# v0.3.28 Debug Completeness

v0.3.28 adds immutable registry, provider dispatch gate, redacted model-input manifest, runtime self-validation, model-input receipt, root/derived errors, packaged handler receipt, stale scan, and normalized error evidence. Full handles, artifact tokens, nonce, OAuth material, cookies, and credentials are not persisted. Provider evidence is not fabricated when dispatch is blocked.
