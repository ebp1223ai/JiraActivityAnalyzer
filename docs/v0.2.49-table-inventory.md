# v0.2.49 Table Inventory

| pageId | tableId | Source | Required columns | Filters / sort | v0.2.49 treatment |
|---|---|---|---|---|---|
| database | databaseIssueList | SQLite `source_objects` + current snapshot | `issueKey` | Server-side | Operational shared table |
| issueViewer | issueActivityEvents | SQLite `activity_events` | `eventTime`, `eventType` | Server-side | Operational shared table + Diff |
| issueViewer | issueChangelog | Compressed current Issue payload | `created`, `field` | Payload-backed | Readable table; schema-v2 limitation retained |
| issueViewer | issueComments | Compressed current Issue payload | `created`, `author` | Payload-backed | Readable cards; schema-v2 limitation retained |
| issueViewer | attachmentsMetadata | Compressed current Issue payload | filename | None | Read-only evidence table |
| issueViewer | issueLinks | Compressed current Issue payload | issue/object | None | Read-only evidence table |
| userViewer | userList | SQLite actor aggregates | `userId`, `displayName` | Server search | Selection list, not a result table |
| userViewer | userRelatedIssues | SQLite activity + snapshot | `issueKey` | Server-side | Operational shared table |
| userViewer | userAllActivityEvents | SQLite `activity_events` | `eventTime`, `issueKey`, `eventType` | Server-side | Operational shared table + Diff |
| collection | timelineEventList | Session activity timeline | time, issue key | Renderer/session model | Existing workflow table |
| collection | selectIssues | Session issue groups | selected, issue key | Renderer/session model | Existing workflow table |
| import | issuePreview | Static prototype | issue key | None | Out of operational Viewer scope |
| timeline | activityEvents | Static prototype | time | None | Legacy static route |
| jiraAnalysis | analysisTables | Analysis result DTO | varies | Tab-specific | Existing analysis viewer |
| jiraProbe | diagnosticTables | Probe response DTO | varies | Inspector search | Diagnostic-only tables |
| activityStreamProbe | diagnosticTables | Probe/session DTO | varies | Tool-specific | Diagnostic-only tables |

Operational Viewer tables use declarative column definitions, required-column enforcement, query-field mapping, server-side paging, table-local overflow, and UI-only preferences. Static prototype and diagnostic tables remain simple `DataTable` instances and do not write operational table preferences.
