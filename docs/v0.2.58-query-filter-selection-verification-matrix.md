# v0.2.58 Query, Filter and Selection Verification Matrix

## Scope

This matrix covers offline, read-only query behavior introduced in v0.2.58. SQLite schema remains v3. No Jira write, database migration, ENV change, or dependency upgrade is included.

| Area | Contract | Automated evidence | Result |
|---|---|---|---|
| Date shortcuts | All Time, Today, Last 7 Days, Last 30 Days, This Month, Custom | `test:v0.2.58` fixed-clock assertions | Pass |
| Date boundary | Local calendar start inclusive, next day exclusive | July boundary synthetic SQLite rows | Pass |
| Invalid range | Start after end blocks query | `DATE_RANGE_INVALID` assertion | Pass |
| Overview date mode | Activity Event, Jira Created, Jira Last Updated | Synthetic three-issue database | Pass |
| Overview distributions | Complete filtered distinct issue set, not current page | Status plus July scope distribution assertion | Pass |
| User period | Related Issues and Activity Events use `event_time` | Synthetic stable user activity | Pass |
| User distributions | Same related-issue query/date scope | Status plus period distribution assertion | Pass |
| Issue Activity Events | Date range applies to `event_time` | In-range/out-of-range issue event assertions | Pass |
| Excel candidates | Searchable checkbox candidates; own filter excluded | Database Project candidate assertion | Pass |
| Excel interaction | Apply, Cancel, Clear Column, Clear All, Select All Search Results, Esc | Source contract plus focused query tests | Pass |
| Changelog table | Full record set filter/sort/count/pagination before page slicing | `queryLocalTable` assertions | Pass |
| Selection | Select/Deselect all filtered results across all pages | Source contract and atomic Set-based implementation | Pass |
| Global selection | Project filtering does not clear selections | Existing Step 2 state contract retained | Pass |
| Description identity | Canonical `fieldId/fieldName === description`, not contains | Positive and negative classifier assertions | Pass |
| Rich content | String, HTML, ADF/object/array/null canonicalization | Focused content assertions | Pass |
| Change states | changed, whitespace-only, unchanged, before/after unavailable, unparseable | Focused classifier assertions | Pass |
| Comments | Created default; Updated fallback to Created; both missing excluded/count shown | UI contract and query-state assertions | Pass |
| Presets | Viewer/tab isolated, duplicate guarded, atomically persisted | Temporary APP_ROOT preference test | Pass |
| Stale response | requestId/revision/cache fingerprint prevents stale replacement | Existing request gates plus typed revision integration | Pass |
| IME | Text input changes are composition-safe; no key transform while composing | v0.2.51 regression | Pass |
| Safety | Parameterized values, allowlisted columns, stable secondary sort | SQLite query implementation and regressions | Pass |

## Deferred manual gates

- Real large SQLite performance and candidate cardinality.
- Windows GUI keyboard/mouse inspection for every tab.
- Installer and Portable launch inspection with real display scaling.
- Real Jira is intentionally not contacted.
