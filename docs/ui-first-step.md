# UI First Step

This version contains static mock pages for Dashboard, Connections, Import, Timeline, Analysis, Jira Analysis, and Settings inside an Electron desktop app shell.

No Jira API, database writes, token storage, backup restore, import, or export behavior is implemented in this first UI pass.
